# Widget Template

This repository hold a simple [Tauri](https://tauri.app/) template to create simple widgets

## Window size

To update the widget size, update the `app.windows.width` & `app.windows.height` values in [tauri.conf.json](src-tauri/tauri.conf.json)